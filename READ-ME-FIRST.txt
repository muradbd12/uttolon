এই আপডেটে ২টা নতুন ফিচার — (১) সত্যিকারের উপস্থিতি (Attendance) সিস্টেম,
(২) গার্ডিয়ান অ্যাকাউন্ট তৈরির সময় কোন শিক্ষার্থীর সাথে যুক্ত তা বেছে
নেওয়া। প্রোফাইল তথ্যও (নাম/ক্লাস/বিষয়) এখন সব ড্যাশবোর্ডে real।

=========================================================
ধাপ ১ — Security Rules আবার সম্পূর্ণ বদলে দিন (গুরুত্বপূর্ণ)
=========================================================
Firestore Database -> Rules -এ গিয়ে সব মুছে এটা বসিয়ে Publish করুন:

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return request.auth != null &&
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    function isTeacher() {
      return request.auth != null &&
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'teacher';
    }

    function isGuardianOf(studentUid) {
      return request.auth != null &&
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.linkedStudentUid == studentUid;
    }

    match /admissions/{applicationId} {
      allow create: if true;
      allow read, update, delete: if isAdmin();
    }

    match /notices/{noticeId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /users/{userId} {
      allow read: if (request.auth != null && request.auth.uid == userId)
        || isAdmin()
        || (isTeacher() && resource.data.role == 'student')
        || isGuardianOf(userId);
      allow write: if false;
    }

    match /attendance/{attendanceId} {
      allow create, update: if isAdmin() || isTeacher();
      allow read: if isAdmin() || isTeacher()
        || (request.auth != null && resource.data.studentUid == request.auth.uid)
        || isGuardianOf(resource.data.studentUid);
    }
  }
}

মানে যোগ হলো: শিক্ষক নিজের স্টুডেন্টদের তালিকা দেখতে পারবেন (উপস্থিতি
নেওয়ার জন্য), গার্ডিয়ান শুধু নিজের যুক্ত সন্তানের তথ্য দেখতে পারবেন,
উপস্থিতি রেকর্ড শুধু Admin/Teacher লিখতে পারবেন।

=========================================================
ফাইল বসানো
=========================================================
সব নতুন/পরিবর্তিত ফাইল (এই zip-এ ১২টা) Merge/Replace করে বসান।
কোনো ফাইল ডিলিট করার দরকার নেই এবার।

=========================================================
এখন যা যা কাজ করবে
=========================================================
- Admin -> "নতুন গার্ডিয়ান" তৈরির সময় এখন "কোন শিক্ষার্থীর সাথে যুক্ত"
  বেছে নিতে হবে (ড্রপডাউনে বিদ্যমান শিক্ষার্থীদের তালিকা দেখাবে)
- Teacher Dashboard -> "উপস্থিতি নিন" বাটন এখন সত্যিকারের কাজ করে —
  সব শিক্ষার্থীর তালিকা দেখাবে, উপস্থিত/অনুপস্থিত বেছে সংরক্ষণ করা যাবে
- Student Dashboard -> উপস্থিতির শতাংশ এখন real (Firestore থেকে হিসাব
  করা)
- Guardian Dashboard -> নিজের যুক্ত সন্তানের নাম/ক্লাস/উপস্থিতি real

Assessment/Homework/Recovery/শিক্ষকের মন্তব্য এখনো demo — এগুলো
পরের ধাপে real করা হবে (এর জন্য আরও বড় একটা কাঠামো লাগবে)।

=========================================================
টেস্ট করবেন যেভাবে
=========================================================
1. আগে একটা Student অ্যাকাউন্ট বানিয়ে থাকলে সেটা দিয়েই চলবে; না
   থাকলে /admin/users দিয়ে একটা বানান
2. Teacher অ্যাকাউন্ট দিয়ে /teacher/login -এ লগইন করুন (Teacher
   অ্যাকাউন্ট না থাকলে আগে /admin/users দিয়ে একটা বানান)
3. Dashboard থেকে "উপস্থিতি নিন" চেপে আজকের তারিখে ১-২ জনকে
   Present/Absent মার্ক করে সংরক্ষণ করুন
4. Student অ্যাকাউন্ট দিয়ে লগইন করে dashboard-এ উপস্থিতির শতাংশ
   দেখুন — real সংখ্যা আসা উচিত
5. একটা Guardian অ্যাকাউন্ট বানিয়ে (সেই স্টুডেন্টের সাথে যুক্ত করে)
   লগইন করে দেখুন সন্তানের নাম ও উপস্থিতি দেখাচ্ছে কিনা
